'use client'
import { useMemo, useState } from 'react'
import { MapPin, Users } from 'lucide-react'
import type { Issue } from '@/lib/db/schema'
export function IssuesList({issues}:{issues:Issue[]}){const[q,setQ]=useState(''),[status,setStatus]=useState('all');const rows=useMemo(()=>issues.filter(x=>(status==='all'||x.status===status)&&`${x.title} ${x.description} ${x.ward} ${x.category}`.toLowerCase().includes(q.toLowerCase())),[issues,q,status]);return <><div className="mb-6 flex flex-col gap-3 rounded-xl border bg-card p-4 sm:flex-row"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search issues" className="h-11 flex-1 rounded-lg border bg-background px-3"/><select value={status} onChange={e=>setStatus(e.target.value)} className="h-11 rounded-lg border bg-background px-3"><option value="all">All statuses</option><option value="pending">Pending</option><option value="in-progress">In progress</option><option value="resolved">Resolved</option></select></div><div className="grid gap-4">{rows.map(x=><article key={x.id} className="rounded-2xl border bg-card p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-primary">{x.category}</p><h2 className="mt-2 font-serif text-xl">{x.title}</h2></div><span className="rounded-full bg-muted px-3 py-1 text-xs font-semibold capitalize">{x.status.replace('-',' ')}</span></div><p className="mt-3 leading-relaxed text-muted-foreground">{x.description}</p><div className="mt-5 flex flex-wrap gap-4 text-sm text-muted-foreground"><span className="flex items-center gap-2"><MapPin className="size-4"/>{x.ward}{x.station?` · ${x.station}`:''}</span><span className="flex items-center gap-2"><Users className="size-4"/>{x.peopleCount} people represented</span></div></article>)}{!rows.length&&<p className="rounded-2xl border bg-card p-10 text-center text-muted-foreground">No issues match these filters.</p>}</div></>}
