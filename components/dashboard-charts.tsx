'use client'
import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import type { Issue } from '@/lib/types'
export function DashboardCharts({issues}:{issues:Issue[]}){const group=(key:'ward'|'category')=>Object.entries(issues.reduce<Record<string,number>>((a,x)=>{a[x[key]]=(a[x[key]]||0)+x.peopleCount;return a},{})).map(([name,value])=>({name,value})).sort((a,b)=>b.value-a.value);const wards=group('ward'),categories=group('category');return <div className="grid gap-5 lg:grid-cols-2"><ChartCard title="Issues by ward" subtitle="People represented by ward"><ResponsiveContainer width="100%" height={280}><BarChart data={wards} layout="vertical" margin={{left:10}}><CartesianGrid strokeDasharray="3 3" horizontal={false}/><XAxis type="number"/><YAxis dataKey="name" type="category" width={90} tick={{fontSize:12}}/><Tooltip/><Bar dataKey="value" fill="var(--chart-2)" radius={[0,5,5,0]}/></BarChart></ResponsiveContainer></ChartCard><ChartCard title="Top categories" subtitle="Share of community concerns"><ResponsiveContainer width="100%" height={280}><PieChart><Pie data={categories} dataKey="value" nameKey="name" innerRadius={60} outerRadius={100} paddingAngle={2}>{categories.map((_,i)=><Cell key={i} fill={i%2?'var(--chart-2)':'var(--chart-1)'}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></ChartCard></div>}
function ChartCard({title,subtitle,children}:{title:string,subtitle:string,children:React.ReactNode}){return <section className="rounded-2xl border bg-card p-5"><h2 className="font-serif text-2xl">{title}</h2><p className="mb-4 text-sm text-muted-foreground">{subtitle}</p>{children}</section>}
