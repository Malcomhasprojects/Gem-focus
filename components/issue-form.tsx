'use client'
import { useState } from 'react'
import { CheckCircle2, Loader2 } from 'lucide-react'
import { createIssue } from '@/app/actions/issues'
import { CATEGORIES, WARDS } from '@/lib/constants'
import { Button } from '@/components/ui/button'
export function IssueForm(){const[done,setDone]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(form:FormData){setBusy(true);setError('');try{await createIssue(form);setDone(true)}catch(e){setError(e instanceof Error?e.message:'Unable to submit issue.')}finally{setBusy(false)}}if(done)return <div className="flex flex-col items-center gap-4 py-14 text-center"><CheckCircle2 className="size-12 text-primary"/><h2 className="font-serif text-3xl">Issue received</h2><p className="text-muted-foreground">Thank you. Your report has been added to the community dashboard.</p><Button onClick={()=>setDone(false)}>Report another issue</Button></div>;return <form action={submit} className="grid gap-5"><Field label="Issue title"><input required minLength={5} name="title" className="h-11 rounded-lg border bg-background px-3" placeholder="Briefly describe the concern"/></Field><Field label="Details"><textarea required minLength={10} name="description" rows={5} className="rounded-lg border bg-background p-3" placeholder="What is happening, who is affected, and for how long?"/></Field><div className="grid gap-5 sm:grid-cols-2"><Field label="Ward"><select name="ward" required className="h-11 rounded-lg border bg-background px-3"><option value="">Select ward</option>{WARDS.map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Village / polling station"><input name="station" className="h-11 rounded-lg border bg-background px-3" placeholder="Optional location detail"/></Field><Field label="Category"><select name="category" required className="h-11 rounded-lg border bg-background px-3"><option value="">Select category</option>{CATEGORIES.map(x=><option key={x}>{x}</option>)}</select></Field><Field label="People who raised this issue"><input name="peopleCount" type="number" min="1" defaultValue="1" required className="h-11 rounded-lg border bg-background px-3"/></Field></div><div className="rounded-xl bg-muted p-5"><p className="mb-4 text-sm font-semibold">Optional contact details — kept private</p><div className="grid gap-5 sm:grid-cols-2"><Field label="Name"><input name="reporterName" className="h-11 rounded-lg border bg-background px-3"/></Field><Field label="Phone"><input name="reporterPhone" type="tel" className="h-11 rounded-lg border bg-background px-3" placeholder="07..."/></Field></div></div>{error&&<p role="alert" className="text-sm text-destructive">{error}</p>}<Button type="submit" size="lg" disabled={busy}>{busy&&<Loader2 className="animate-spin" data-icon="inline-start"/>}Submit community issue</Button></form>}
function Field({label,children}:{label:string,children:React.ReactNode}){return <label className="flex flex-col gap-2 text-sm font-medium">{label}{children}</label>}
