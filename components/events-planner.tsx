'use client'
import { useMemo, useState } from 'react'
import { CalendarDays, Clock3, MapPin, Users } from 'lucide-react'
import { format } from 'date-fns'
import type { CampaignEvent } from '@/lib/db/schema'
import { WARDS } from '@/lib/constants'
export function EventsPlanner({events}:{events:CampaignEvent[]}){const[ward,setWard]=useState('all'),[status,setStatus]=useState('all');const rows=useMemo(()=>events.filter(x=>(ward==='all'||x.ward===ward)&&(status==='all'||x.status===status)),[events,ward,status]);return <><div className="mb-6 flex flex-col gap-3 rounded-xl border bg-card p-4 sm:flex-row sm:items-center"><strong className="mr-auto flex items-center gap-2"><CalendarDays className="size-4 text-primary"/>Campaign schedule</strong><select value={ward} onChange={e=>setWard(e.target.value)} className="h-10 rounded-lg border bg-background px-3"><option value="all">All wards</option>{WARDS.map(x=><option key={x}>{x}</option>)}</select><select value={status} onChange={e=>setStatus(e.target.value)} className="h-10 rounded-lg border bg-background px-3"><option value="all">All statuses</option><option value="scheduled">Scheduled</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select></div><div className="grid gap-5 md:grid-cols-2">{rows.map(x=><article key={x.id} className="flex flex-col rounded-2xl border bg-card p-5"><div className="flex items-start justify-between gap-3"><div><span className="text-xs font-bold uppercase tracking-wider text-primary">{x.ward}</span><h2 className="mt-2 font-serif text-xl">{x.title}</h2></div><span className="rounded-full bg-muted px-3 py-1 text-xs font-semibold capitalize">{x.status}</span></div><div className="my-5 flex flex-col gap-2 rounded-xl bg-muted p-4 text-sm text-muted-foreground"><span className="flex items-center gap-2"><CalendarDays className="size-4"/>{format(new Date(`${x.eventDate}T12:00:00`),'EEEE, MMMM d, yyyy')}</span><span className="flex items-center gap-2"><Clock3 className="size-4"/>{x.eventTime.slice(0,5)}</span><span className="flex items-center gap-2"><MapPin className="size-4"/>{x.venue}</span></div><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Campaign objective</p><p className="mt-2 flex-1 text-sm italic leading-relaxed text-muted-foreground">{x.objective}</p><p className="mt-5 flex items-center gap-2 border-t pt-4 text-sm font-semibold"><Users className="size-4"/>{x.status==='completed'?`Attendance: ${x.attendance??0}`:`Expected: ${x.expectedAttendance??'N/A'}`}</p></article>)}{!rows.length&&<p className="rounded-2xl border bg-card p-10 text-center text-muted-foreground md:col-span-2">No events match these filters.</p>}</div></>}
