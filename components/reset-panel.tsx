'use client'
import { useState } from 'react'
import { AlertTriangle } from 'lucide-react'
import { resetAll } from '@/app/actions/admin'
import { Button } from '@/components/ui/button'
export function ResetPanel(){const[open,setOpen]=useState(false);return <section className="rounded-2xl border border-destructive/40 bg-card p-5"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><h2 className="font-serif text-2xl text-destructive">Danger zone</h2><p className="text-sm text-muted-foreground">Permanently clear issues, events, attendance, and campaign metrics.</p></div><Button variant="destructive" onClick={()=>setOpen(true)}>Reset all data</Button></div>{open&&<form action={resetAll} className="mt-5 rounded-xl border border-destructive/40 p-5"><p className="flex gap-3 text-destructive"><AlertTriangle className="size-6 shrink-0"/><strong>Are you sure you want to reset every data value to 0? This permanently deletes all issue and event records and cannot be undone.</strong></p><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="flex flex-col gap-2 text-sm font-medium">Type RESET ALL DATA<input name="confirmation" required className="h-10 rounded-lg border bg-background px-3"/></label><label className="flex flex-col gap-2 text-sm font-medium">Re-enter admin key<input name="key" type="password" required className="h-10 rounded-lg border bg-background px-3"/></label></div><div className="mt-4 flex gap-3"><Button type="submit" variant="destructive">Permanently reset</Button><Button type="button" variant="outline" onClick={()=>setOpen(false)}>Cancel</Button></div></form>}</section>}
